// Type Integer & Decimal 
const int = 42;
const des = 15.26;
console.log(typeof int, typeof des); // There is no distinction

// Scientific Notation
const sN = 7e10;

// Infinite & NaN (Not a Number)
const inf = Infinity;
const naN = NaN;

// Arithmetic operations
const acO = {
    s: 5 + 5,
    r: 5 - 5,
    m: 5 * 5,
    d: 5 / 5,
    e: 5 ** 5,
    mO: 5 % 5
};
console.log(acO);
    // Precision Results
    const opc = 0.2 + 0.1;
    console.log(opc);
    console.log(opc.toFixed(1));

// Avanced operations
const aO = {
    aO1: Math.sqrt(25),
    aO2: Math.abs(-15),
    aO3: Math.random()
};


